const ADDRESS = {
    NODE: import.meta.env.REACT_APP_NODE_ADDRESS,
};

const LOCAL_STORAGE = {
    ACCOUNT: 'account',
};

const PROGRAM = {
    PROGRAM_ID: import.meta.env.VITE_PROGRAM_ID    
}

const METADATA = {
    META: import.meta.env.VITE_METADATA  
}

export { ADDRESS, LOCAL_STORAGE, PROGRAM, METADATA };